#include <unistd.h>

/**
 * _putchar - writes the character c to stdout
 * @c: The character to prineturned, and errno is set appropriately.
 */
int _t
 *
 * Return: On success 1.
 * On error, -1 is rputchar(char c)
{
return (write(1, &c, 1));
}
