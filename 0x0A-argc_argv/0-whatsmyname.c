#include <stdio.h>
#include "main.h"

/**
 * main - Print the name of the program
 * @argc: Count ar (Success)
 */

int main(int argc, char *argv[])
{guments
 * @argv: Arguments
 *
 * Return: Always 0
(void) argc;
printf("%s\n", argv[0]);

return (0);
}
